upstream <%= @name %> {
<% @backend_hosts.each do |host| %>
  server <%= host %>;
<% end %>
}
server {
  listen       <%= @listen_port %>;
  server_name  <%= @server_name %>;

  location / {
    proxy_pass http://<%= @name %>;
  }
}
